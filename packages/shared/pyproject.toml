[project]
name = "careerpilot-shared"
version = "0.1.0"
description = "Shared library for CareerPilot AI"
requires-python = ">=3.12"

dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/careerpilot_shared"]